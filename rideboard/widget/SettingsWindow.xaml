<Window x:Class="RideBoard.Widget.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Settings" Height="280" Width="300"
        WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="CenterScreen">
    
    <Border CornerRadius="6" Background="{StaticResource PanelBrush}" BorderBrush="#333333" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>
        <Grid>
            <!-- Scanline Overlay -->
            <Rectangle Fill="{StaticResource ScanlineBrush}" Opacity="0.3" IsHitTestVisible="False" RadiusX="6" RadiusY="6"/>
            
            <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="SETTINGS" Foreground="{StaticResource LabelBrush}" FontSize="16" FontFamily="{StaticResource PixelFont}" HorizontalAlignment="Center"/>

            <!-- Content -->
            <StackPanel Grid.Row="1" VerticalAlignment="Center">
                <!-- Auto Start Checkbox -->
                <CheckBox Content="START ON BOOT" 
                          IsChecked="{Binding IsAutoStartEnabled}"
                          Foreground="{StaticResource DigitalMainBrush}" FontSize="14" FontFamily="{StaticResource PixelFont}"
                          HorizontalAlignment="Center"
                          Cursor="Hand"
                          Margin="0,0,0,15"/>

                <!-- Strava Login Button -->
                <Button Content="CONNECT STRAVA"
                        Command="{Binding LoginCommand}"
                        Background="#FC4C02" Foreground="White" FontFamily="{StaticResource PixelFont}" FontSize="14"
                        Padding="15,6" BorderThickness="0"
                        HorizontalAlignment="Center"
                        Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="0"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <!-- About Info -->
                <StackPanel Margin="0,25,0,0">
                    <TextBlock Text="RIDEBOARD v1.2.3.1" Foreground="{StaticResource LabelBrush}" FontSize="14" FontFamily="{StaticResource PixelFont}" HorizontalAlignment="Center"/>
                    <TextBlock Foreground="{StaticResource DigitalDarkBrush}" FontSize="12" FontFamily="{StaticResource PixelFont}" HorizontalAlignment="Center" Margin="0,4,0,0">
                        <Hyperlink NavigateUri="https://github.com/raymond89huang-prog/strava_ride_widget" RequestNavigate="Hyperlink_RequestNavigate"
                                   Foreground="{StaticResource DigitalDarkBrush}" TextDecorations="None">
                            DEVELOPED BY RAYMOND HUANG
                        </Hyperlink>
                    </TextBlock>
                    <TextBlock Text="2025 OPEN SOURCE (MIT)" Foreground="#60FFFFFF" FontSize="10" FontFamily="{StaticResource PixelFont}" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                </StackPanel>
            </StackPanel>

            <!-- Close Button -->
            <Button Grid.Row="2" Content="CLOSE"
                    Click="BtnClose_Click"
                    Background="Transparent" Foreground="{StaticResource LabelBrush}" FontFamily="{StaticResource PixelFont}"
                    BorderThickness="0" FontSize="14"
                    HorizontalAlignment="Center"
                    Cursor="Hand" Margin="0,10,0,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>
