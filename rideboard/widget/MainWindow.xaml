<Window x:Class="RideBoard.Widget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RideBoard" Width="540" Height="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="False"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Loaded="Window_Loaded" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="{StaticResource LabelGrey}"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource DigitalAmber}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.LayoutTransform>
            <ScaleTransform ScaleX="1.5" ScaleY="1.5"/>
        </Grid.LayoutTransform>
        <Border CornerRadius="6" BorderBrush="#333333" BorderThickness="1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#CC141414" Offset="0.0"/>
                    <GradientStop Color="#CC1E1E1E" Offset="1.0"/>
                </LinearGradientBrush>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="15" ShadowDepth="5" Opacity="0.7"/>
            </Border.Effect>
            
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="RideBoard" Foreground="{StaticResource LabelGrey}" FontSize="12" FontFamily="{StaticResource LabelFont}" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="BtnSettings" Style="{StaticResource NavButtonStyle}" FontSize="14" Margin="0,0,8,0" Click="BtnSettings_Click" ToolTip="Settings" SnapsToDevicePixels="True">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets"/>
                        </Button>
                        <Button x:Name="BtnClose" Content="✕" Style="{StaticResource NavButtonStyle}" FontSize="12" Click="BtnClose_Click" ToolTip="Hide Widget"/>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Height="1" Background="#333333" Margin="0,0,0,12"/>

                <Grid Grid.Row="2">
                    <StackPanel Visibility="{Binding PageDailyVisibility}">
                        <Grid Margin="0,0,0,12">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <TextBlock Text="LAST RIDE" Foreground="{StaticResource LabelGrey}" FontSize="10" FontFamily="{StaticResource LabelFont}" FontWeight="Bold"/>
                            
                            <Grid Grid.Row="1" Margin="0,2,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Bottom">
                                    <TextBlock Text="{Binding LastDistance}" Foreground="{StaticResource DigitalAmber}" FontSize="24" FontFamily="{StaticResource DigitalFont}">
                                        <TextBlock.Effect>
                                            <StaticResource ResourceKey="DigitalGlow"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                    <TextBlock Text=" km" Foreground="{StaticResource DigitalDim}" FontSize="12" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="3,0,0,4"/>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding LastTime}" Foreground="{StaticResource DigitalAmber}" FontSize="20" FontFamily="{StaticResource DigitalFont}" HorizontalAlignment="Right">
                                        <TextBlock.Effect>
                                            <StaticResource ResourceKey="DigitalGlow"/>
                                        </TextBlock.Effect>
                                    </TextBlock>
                                    
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding LastPower}" Foreground="{StaticResource DigitalAmber}" FontSize="16" FontFamily="{StaticResource DigitalFont}">
                                            <TextBlock.Effect>
                                                <StaticResource ResourceKey="DigitalGlow"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text=" W " Foreground="{StaticResource DigitalDim}" FontSize="10" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="2,0,8,3"/>
                                        
                                        <TextBlock Text="{Binding LastElev}" Foreground="{StaticResource DigitalAmber}" FontSize="16" FontFamily="{StaticResource DigitalFont}">
                                            <TextBlock.Effect>
                                                <StaticResource ResourceKey="DigitalGlow"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text=" m" Foreground="{StaticResource DigitalDim}" FontSize="10" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Grid>

                        <Border Height="1" Background="#252525" Margin="0,4,0,8"/>

                        <Grid>
                             <TextBlock Text="THIS WEEK" Foreground="{StaticResource LabelGrey}" FontSize="10" FontFamily="{StaticResource LabelFont}" FontWeight="Bold"/>
                             <Grid Margin="0,10,0,0">
                                 <Grid.ColumnDefinitions>
                                     <ColumnDefinition Width="*"/>
                                     <ColumnDefinition Width="Auto"/>
                                 </Grid.ColumnDefinitions>
                                 
                                 <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Top">
                                     <TextBlock Text="{Binding WeekDistance}" Foreground="{StaticResource DigitalAmber}" FontSize="24" FontFamily="{StaticResource DigitalFont}">
                                         <TextBlock.Effect>
                                             <StaticResource ResourceKey="DigitalGlow"/>
                                         </TextBlock.Effect>
                                     </TextBlock>
                                     <TextBlock Text=" km" Foreground="{StaticResource DigitalDim}" FontSize="12" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="3,0,0,5"/>
                                 </StackPanel>
                                 
                                 <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                     <TextBlock Text="{Binding WeekTime}" Foreground="{StaticResource DigitalAmber}" FontSize="20" FontFamily="{StaticResource DigitalFont}" HorizontalAlignment="Right">
                                         <TextBlock.Effect>
                                             <StaticResource ResourceKey="DigitalGlow"/>
                                         </TextBlock.Effect>
                                     </TextBlock>
                                     
                                     <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,2,0,0">
                                         <TextBlock Text="{Binding WeekElev}" Foreground="{StaticResource DigitalAmber}" FontSize="16" FontFamily="{StaticResource DigitalFont}">
                                             <TextBlock.Effect>
                                                 <StaticResource ResourceKey="DigitalGlow"/>
                                             </TextBlock.Effect>
                                         </TextBlock>
                                         <TextBlock Text=" m" Foreground="{StaticResource DigitalDim}" FontSize="10" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                                     </StackPanel>
                                 </StackPanel>
                             </Grid>
                        </Grid>
                    </StackPanel>

                    <StackPanel Visibility="{Binding PageYearlyVisibility}">
                        <TextBlock Text="YEARLY SUMMARY" Foreground="{StaticResource LabelGrey}" FontWeight="Bold" FontSize="11" FontFamily="{StaticResource LabelFont}" Margin="0,0,0,2"/>
                        <TextBlock Text="{Binding YearRange}" Foreground="#50FFFFFF" FontSize="10" FontFamily="{StaticResource DigitalFont}" Margin="0,0,0,16"/>
                        
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Total Distance" Foreground="{StaticResource LabelGrey}" FontSize="11" FontFamily="{StaticResource LabelFont}" VerticalAlignment="Top"/>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding YearTotalDistance}" Foreground="{StaticResource DigitalAmber}" FontSize="28" FontFamily="{StaticResource DigitalFont}" FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <StaticResource ResourceKey="DigitalGlow"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text=" km" Foreground="{StaticResource DigitalDim}" FontSize="12" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                            </StackPanel>
                            <Border BorderBrush="#252525" BorderThickness="0,0,0,1" VerticalAlignment="Bottom" Margin="0,32,0,0"/>
                        </Grid>

                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Total Elevation" Foreground="{StaticResource LabelGrey}" FontSize="11" FontFamily="{StaticResource LabelFont}" VerticalAlignment="Top"/>
                             <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding YearTotalElev}" Foreground="{StaticResource DigitalAmber}" FontSize="28" FontFamily="{StaticResource DigitalFont}" FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <StaticResource ResourceKey="DigitalGlow"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                                <TextBlock Text=" m" Foreground="{StaticResource DigitalDim}" FontSize="12" FontFamily="{StaticResource DigitalFont}" VerticalAlignment="Bottom" Margin="4,0,0,6"/>
                            </StackPanel>
                            <Border BorderBrush="#252525" BorderThickness="0,0,0,1" VerticalAlignment="Bottom" Margin="0,32,0,0"/>
                        </Grid>

                        <Grid>
                            <TextBlock Text="Total Moving Time" Foreground="{StaticResource LabelGrey}" FontSize="11" FontFamily="{StaticResource LabelFont}" VerticalAlignment="Top"/>
                             <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="{Binding YearTotalTime}" Foreground="{StaticResource DigitalAmber}" FontSize="28" FontFamily="{StaticResource DigitalFont}" FontWeight="Bold">
                                    <TextBlock.Effect>
                                        <StaticResource ResourceKey="DigitalGlow"/>
                                    </TextBlock.Effect>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                </Grid>

                <Grid Grid.Row="3" Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                        <Button Command="{Binding PrevPageCommand}" Content="‹" Style="{StaticResource NavButtonStyle}"/>
                        <Button Command="{Binding NextPageCommand}" Content="›" Style="{StaticResource NavButtonStyle}" Margin="6,0,0,0"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                         <Button Command="{Binding RefreshCommand}" Content="⟳" Style="{StaticResource NavButtonStyle}" 
                                 FontSize="12" Margin="0,0,8,0" ToolTip="Refresh Now"/>
                         <TextBlock Text="{Binding FooterText}" Foreground="{StaticResource LabelGrey}" FontSize="10" FontFamily="{StaticResource LabelFont}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>

                <!-- Offline Overlay (Covering only data area, keeping Header/Footer accessible) -->
                <Border Grid.Row="2" Background="#CC000000" Visibility="{Binding OfflineVisibility}" CornerRadius="4">
                    <TextBlock Text="OFFLINE" Foreground="{StaticResource DigitalAmber}" FontFamily="{StaticResource DigitalFont}" FontWeight="Bold" FontSize="24"
                               HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Effect>
                            <StaticResource ResourceKey="DigitalGlow"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Window>
