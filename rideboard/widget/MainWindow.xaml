<Window x:Class="RideBoard.Widget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="RideBoard" Width="360" Height="280"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True"
        ResizeMode="NoResize" ShowInTaskbar="False"
        Loaded="Window_Loaded" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Window.Resources>
        <Style x:Key="NavButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#40FFFFFF"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Border CornerRadius="12" Background="#CC1E1E1E" Padding="16">
            <Border.Effect>
                <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.4"/>
            </Border.Effect>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Header -->
                    <RowDefinition Height="*"/>    <!-- Content -->
                    <RowDefinition Height="Auto"/> <!-- Footer -->
                </Grid.RowDefinitions>

                <!-- 1. Header -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="RideBoard" Foreground="#80FFFFFF" FontSize="12" VerticalAlignment="Center"/>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button x:Name="BtnSettings" Content="⚙" Foreground="#80FFFFFF" Background="Transparent" BorderThickness="0" 
                                FontSize="12" Cursor="Hand" Margin="0,0,8,0" Click="BtnSettings_Click"
                                ToolTip="Settings"/>
                        <Button x:Name="BtnClose" Content="✕" Foreground="#80FFFFFF" Background="Transparent" BorderThickness="0" 
                                FontSize="12" Cursor="Hand" Click="BtnClose_Click"
                                ToolTip="Hide Widget"/>
                    </StackPanel>
                </Grid>

                <!-- 2. Main Content Area (Pages) -->
                <Grid Grid.Row="1" Margin="0,8,0,0">
                    <!-- Page 1: Daily/Weekly -->
                    <StackPanel Visibility="{Binding PageDailyVisibility}">
                        <!-- Today -->
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="TODAY" Foreground="#FFFFFF" FontWeight="Bold" FontSize="14"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="{Binding TodayDistance}" Foreground="#FFFFFF" FontSize="20" FontWeight="Bold"/>
                                <TextBlock Text="{Binding TodayTime}" Foreground="#B0FFFFFF" FontSize="14" VerticalAlignment="Bottom" Margin="12,0,0,0"/>
                                <TextBlock Text="{Binding TodayElev}" Foreground="#80FFFFFF" FontSize="12" VerticalAlignment="Bottom" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- Last Ride -->
                        <StackPanel Orientation="Vertical" Margin="0,8,0,0">
                            <TextBlock Text="LAST RIDE" Foreground="#FFFFFF" FontWeight="Bold" FontSize="14"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="{Binding LastDistance}" Foreground="#FFFFFF" FontSize="16"/>
                                <TextBlock Text="{Binding LastPower}" Foreground="#B0FFFFFF" FontSize="12" VerticalAlignment="Bottom" Margin="12,0,0,0"/>
                                <TextBlock Text="{Binding LastHr}" Foreground="#80FFFFFF" FontSize="12" VerticalAlignment="Bottom" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>

                        <!-- This Week -->
                        <StackPanel Orientation="Vertical" Margin="0,8,0,0">
                            <TextBlock Text="THIS WEEK" Foreground="#FFFFFF" FontWeight="Bold" FontSize="14"/>
                            <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                <TextBlock Text="{Binding WeekDistance}" Foreground="#FFFFFF" FontSize="16"/>
                                <TextBlock Text="{Binding WeekTime}" Foreground="#B0FFFFFF" FontSize="12" VerticalAlignment="Bottom" Margin="12,0,0,0"/>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>

                    <!-- Page 2: Yearly Summary -->
                    <StackPanel Visibility="{Binding PageYearlyVisibility}">
                        <TextBlock Text="YEARLY SUMMARY" Foreground="#FFFFFF" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                        
                        <!-- Year Range -->
                        <TextBlock Text="{Binding YearRange}" Foreground="#B0FFFFFF" FontSize="12" Margin="0,0,0,12"/>

                        <!-- Total Distance -->
                        <TextBlock Text="Total Distance" Foreground="#80FFFFFF" FontSize="11"/>
                        <TextBlock Text="{Binding YearTotalDistance}" Foreground="#FFFFFF" FontSize="24" FontWeight="Bold" Margin="0,0,0,8"/>

                        <!-- Total Elevation -->
                        <TextBlock Text="Total Elevation" Foreground="#80FFFFFF" FontSize="11"/>
                        <TextBlock Text="{Binding YearTotalElev}" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold" Margin="0,0,0,8"/>

                        <!-- Total Time -->
                        <TextBlock Text="Total Moving Time" Foreground="#80FFFFFF" FontSize="11"/>
                        <TextBlock Text="{Binding YearTotalTime}" Foreground="#FFFFFF" FontSize="18" FontWeight="Bold"/>
                    </StackPanel>

                </Grid>

                <!-- 3. Footer -->
                <Grid Grid.Row="2" Margin="0,8,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Navigation Buttons moved to Footer (bottom-left) to avoid content overlap -->
                    <StackPanel Orientation="Horizontal" Grid.Column="0">
                        <Button Command="{Binding PrevPageCommand}" Content="‹" Style="{StaticResource NavButtonStyle}"/>
                        <Button Command="{Binding NextPageCommand}" Content="›" Style="{StaticResource NavButtonStyle}" Margin="6,0,0,0"/>
                    </StackPanel>

                    <!-- Refresh Button (Hidden but clickable area over time text, or separate icon) -->
                    <!-- Let's add a small refresh icon next to time -->
                    <StackPanel Orientation="Horizontal" Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                         <Button Command="{Binding RefreshCommand}" Content="⟳" Style="{StaticResource NavButtonStyle}" 
                                 FontSize="12" Margin="0,0,8,0" ToolTip="Refresh Now"/>
                         <TextBlock Text="{Binding FooterText}" Foreground="#80FFFFFF" FontSize="11" VerticalAlignment="Center"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>

        <Border Background="#66000000" Visibility="{Binding OfflineVisibility}">
            <TextBlock Text="OFFLINE / SERVICE NOT RUNNING" Foreground="#FFFFFF" FontWeight="Bold" FontSize="14"
                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </Border>
    </Grid>
</Window>
